{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "ingredients": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1
          },
          "quantity": {
            "type": "number",
            "minimum": 0
          },
          "unit": {
            "type": "string",
            "enum": ["g", "ml", "piece"]
          }
        },
        "required": ["name", "quantity", "unit"]
      }
    },
    "total_mass_g": {
      "type": "number",
      "minimum": 0
    }
  },
  "required": ["ingredients", "total_mass_g"]
}